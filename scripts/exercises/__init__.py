"""Helper scripts for the dbt practice exercises (docs/exercises/).

Each ``generate_NN_*.py`` produces a deterministic CSV under
``data/exercises/inbox/`` so that learners can ingest a realistic file
without depending on randomness from prior runs.
"""
